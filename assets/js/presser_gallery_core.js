jQuery(document).ready(function($) {
   
});

lightGallery(document.getElementById('presser_gallery-lightgallery'), {
    thumbnail: true,
    plugins: [lgZoom, lgThumbnail],
    licenseKey: 'your_license_key',
    speed: 500,
    // ... other settings
});
